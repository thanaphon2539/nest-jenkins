FROM jenkins/jenkins:lts

USER root

# ติดตั้ง docker CLI + compose plugin
RUN apt-get update && \
    apt-get install -y docker.io docker-compose-plugin curl && \
    rm -rf /var/lib/apt/lists/*

USER jenkins
